---
import WelcomeImageByTime from "./WelcomeImageByTime";
import { Button } from "@/components/ui/button";
import { IoLanguageOutline } from "react-icons/io5";
import {ToggleDarkMode} from "./ToggleDarkMode";
---

<header class="dark:bg-slate-900 h-12 rounded mt-6 flex justify-between">
    <div class="flex h-full gap-4 items-center">
        <WelcomeImageByTime client:load />
        <span class="w-1 h-full bg-teal-400 inline-block rounded animate-navbarBar"></span>
    </div>

    <nav class="flex items-center gap-8 px-4">
        <p>Projects</p>
        <p>Experience</p>
        <p>About me</p>
        <div class="flex items-center gap-1">
            <Button>
                <IoLanguageOutline />
            </Button>
            <ToggleDarkMode client:load />
        </div>
    </nav>
</header>
